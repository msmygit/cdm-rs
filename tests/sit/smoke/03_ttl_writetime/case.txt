# Java: SIT/smoke/03_ttl_writetime — migrateData, validateData, breakData.cql, fixData.
#
# Eight records whose four columns carry deliberately different TTLs and writetimes, so that
# the per-row maximum over the *eligible* columns (FEA-040..FEA-046) is a different column for
# each one.
blocked the migrate half works; validate's autocorrect does not. VAL-018 requires a corrected row to carry the origin's TTL and writetime and cdm-rs drops them at three sites, all outside cdm-engine: build::validate resolves no WritetimeTtlPlan so the origin projection selects no TTL()/WRITETIME(); it builds its TargetUpsert from StatementOptions::default() so there is no USING clause; and CqlRowSink::write binds BindInputs { key, ..default() } so both markers would bind UNSET even if there were. expected.out wants 1087384200000000 on the repaired rows and gets the wall clock. Wiring, in cdm-cli::harness::build and cdm-cql::rows -- see docs/ROADMAP.md #21d

# Not runnable yet — see docs/ROADMAP.md #21d. The case below is complete and is
# expected to pass unchanged once the wiring lands.

cql   setup.cql
job   migrate  migrate.properties  migrate.assert
job   validate migrate.properties  validate.assert
cql   break.cql
job   validate fix.properties      fix.assert
check expected.cql expected.out
