# Java: SIT/smoke/03_ttl_writetime — migrateData, validateData, breakData.cql, fixData.
#
# Eight records whose four columns carry deliberately different TTLs and writetimes, so that
# the per-row maximum over the *eligible* columns (FEA-040..FEA-046) is a different column for
# each one.

cql   setup.cql
job   migrate  migrate.properties  migrate.assert
job   validate migrate.properties  validate.assert
cql   break.cql
job   validate fix.properties      fix.assert
check expected.cql expected.out
