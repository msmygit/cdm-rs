# Java: SIT/features/03_codec/execute.sh — migrateData, validateData, breakData.cql, fixData.

# Not runnable yet — see docs/ROADMAP.md #21a. The case below is complete and is
# expected to pass unchanged once the wiring lands.
blocked cdm-cli builds the codec registry as CodecRegistry::with_builtins(&enabled, None) (crates/cdm-cli/src/harness/build.rs::codec_planner), so the codec names are read but the format options are not, and TIMESTAMP_STRING_FORMAT is rejected at startup even when both are configured

cql   setup.cql
job   migrate  migrate.properties  migrate.assert
job   validate migrate.properties  validate.assert
cql   break.cql
job   validate fix.properties      fix.assert
check expected.cql expected.out
