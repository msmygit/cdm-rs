# Java: SIT/features/02_explode_map/execute.sh — migrateData, validateData, breakData.cql, fixData.

# Not runnable yet — see docs/ROADMAP.md #21a. The case below is complete and is
# expected to pass unchanged once the wiring lands.
blocked cdm-cli resolves the mapping with MappingOptions::default(), whose explode_map field is None, so feature.explodeMap.* is ignored and the run fails at startup with SCH-006 on the target's fruit key column

cql   setup.cql
job   migrate  migrate.properties  migrate.assert
job   validate migrate.properties  validate.assert
cql   break.cql
job   validate fix.properties      fix.assert
check expected.cql expected.out
