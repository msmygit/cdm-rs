[package]
name = "cdm-metrics"
description = "Counter registry, instruments, event bus and exporters (Prometheus, OTLP, NDJSON, TUI)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
keywords.workspace = true
categories.workspace = true

[lints]
workspace = true

# The instrument, exporter and event-bus dependencies (metrics, opentelemetry, tokio, ratatui)
# arrive with PRs #36-#39; the counter core needs none of them.
[dependencies]
cdm-core.workspace = true
chrono.workspace = true
tracing.workspace = true

[dev-dependencies]
insta.workspace = true
